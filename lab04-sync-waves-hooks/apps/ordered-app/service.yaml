apiVersion: v1
kind: Service
metadata:
  name: ordered-app
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ClusterIP
  selector:
    app: ordered-app
  ports:
    - name: http
      port: 80
      targetPort: 80
