apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
labels:
  - pairs:
      env: stage
    includeSelectors: false
images:
  - name: nginx
    newTag: 1.28-alpine
patches:
  - path: replica-patch.yaml
    target:
      kind: Deployment
      name: demo-kustomize
